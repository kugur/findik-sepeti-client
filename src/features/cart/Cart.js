import React from "react";
import { Container } from "react-bootstrap";

export const Cart = (props) => {
    return (
        <Container>
            <h1>Cart sayfasi</h1>
        </Container>
    )
}